import React from 'react';
import UnderDevelopmentPlaceholder from '../admin/UnderDevelopmentPlaceholder';

/**
 * Angular `messageTemplateEditorCtrl` + `MessageTemplateEditor.cshtml` — visual drag-and-drop template designer.
 * Not migrated; use Message Template Management → Edit Code for HTML/text templates.
 */
const MessageTemplateLayoutEditor: React.FC = () => (
  <UnderDevelopmentPlaceholder title="Message Template Layout Editor (visual designer)" />
);

export default MessageTemplateLayoutEditor;
